{{#if products.length}}

<ul>
    
    {{#each products}}
    <h3>Producto:</h3>
    <li>{{this.title}}</li>
    <li>{{this.description}}</li>
    <li>{{this.price}}</li>
    {{/each}}
</ul>
{{else}}
<p>No hay productos</p>
{{/if}}

